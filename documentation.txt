no need of simple project structure just code looks like handwritten code must be something like developer not beginner and not so much professional 
project structure must be look like little bit professional and proper because they are counting marks of project structure

this is the full documentation of this project you read it properly and let start

1️⃣ First: Test API connection (simple UI)
2️⃣ Second: Implement all API calls (CRUD)
3️⃣ Third: Build proper UI

provide this all pages with simplest code it looks like handwritten code not ai generated because faculty will check plagrism so remember that it look like write by coder not by ai simplest code structure and code.

Clinic Queue Management API
 1.0.0 
OAS 3.0
Task
Clinic Queue Management System (CMS) is a full-stack, multi-tenant clinic management API. It supports multiple clinics; each clinic has its own admin, doctors, receptionists, and patients. All data is scoped by clinic – users only see and act on data for their assigned clinic.

Student accounts: Each student receives a unique username and password that is already linked to a specific clinic. When implementing the frontend, students must build the UI and flows for their assigned clinic only, using the credentials they receive.

Student login (for frontend implementation):

Username: 24010101666@darshan.ac.in
Password: password123
Authentication
Login: POST /auth/login with email and password. Returns a JWT and user object (id, name, email, role, clinicId, clinicName, clinicCode).
Protected endpoints: Send the token in the header: Authorization: Bearer <token>.
JWT payload includes userId, role, and clinicId. The API uses these to enforce role-based access and clinic isolation.
No public registration. Clinics and admins are created via database seed or backend script; admins create receptionists, doctors, and patients from the Admin UI. New users sign in with the email/password set by the admin.
Errors: 401 = missing or invalid token; 403 = valid token but role not allowed or user has no clinic.

Roles & Capabilities
Role	Description	Main actions
Admin	Owns one clinic.	View clinic info and counts; list users; create receptionists, doctors, and patients (name, email, password, role).
Patient	Belongs to one clinic.	Book appointments; view my appointments (with queue token/status); view appointment details with prescription and report; view my prescriptions and my reports.
Receptionist	Belongs to one clinic.	Get daily queue for a date (date=YYYY-MM-DD); update queue entry status (waiting → in-progress/skipped, in-progress → done).
Doctor	Belongs to one clinic.	Get today's queue (token, patient, appointmentId); add prescription (medicines, notes) and report (diagnosis, tests, remarks) for an appointment.
Each role can access only its allowed endpoints. Data is always filtered by the user's clinicId.

Key Concepts
Clinic: Each clinic has a name and a unique code (e.g. STUD-01). Used for multi-tenant isolation.
Appointment: Patient books a date and time slot. One appointment per slot per clinic per date. On creation, status is set to queued and a queue entry is created.
Queue: One queue entry per appointment per day. Has a token number (per clinic per date), and status: waiting | in_progress | done | skipped. Receptionist advances status; doctor uses appointmentId to add prescription/report.
Prescription / Report: One prescription and one report per appointment, added by the doctor (usually when status is in_progress or done).
Project Walkthrough Video
Watch the project explanation and walkthrough here: Project walkthrough video

Typical Workflows
Admin: Log in → Get clinic info (GET /admin/clinic) → List users (GET /admin/users) → Create doctor/receptionist/patient (POST /admin/users).
Patient: Log in → Book appointment (POST /appointments) → List my appointments (GET /appointments/my) → Get details with prescription/report (GET /appointments/:id); optionally list /prescriptions/my and /reports/my.
Receptionist: Log in → Get queue for a date (GET /queue?date=YYYY-MM-DD) → Update status (PATCH /queue/:id) as patients are called or completed.
Doctor: Log in → Get today's queue (GET /doctor/queue) → For each patient, add prescription (POST /prescriptions/:appointmentId) and/or report (POST /reports/:appointmentId).
Servers
Use the Production Server at (https://cmsback.sampaarsh.cloud) . All paths are relative to the selected server.

Servers

https://cmsback.sampaarsh.cloud - Production (Sampaarsh)

Authorize
Auth
Login (no public registration)



GET
/health
Health check

Parameters
Try it out
No parameters

Responses
Code	Description	Links
200	
OK

Media type

application/json
Controls Accept header.
Example Value
Schema
{
  "ok": true
}
No links

POST
/auth/login
Login

Returns JWT and user info. Use token in Authorization: Bearer header for all other endpoints. No public registration.

Parameters
Cancel
Reset
No parameters

Request body

application/json
Edit Value
Schema
{
  "email": "24010101666@darshan.ac.in",
  "password": "password123"
}
Execute
Clear
Responses
Curl

curl -X 'POST' \
  'https://cmsback.sampaarsh.cloud/auth/login' \
  -H 'accept: application/json' \
  -H 'Content-Type: application/json' \
  -d '{
  "email": "24010101666@darshan.ac.in",
  "password": "password123"
}'
Request URL
https://cmsback.sampaarsh.cloud/auth/login
Server response
Code	Details
200	
Response body
Download
{
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJ1c2VySWQiOjE2NCwicm9sZSI6ImFkbWluIiwiY2xpbmljSWQiOjMzNywiaWF0IjoxNzczNzE4MjE3LCJleHAiOjE3NzQzMjMwMTd9.4sGyCJJyJXNq3hbakDF9GD6TBakrpfsFTjk0pHTfD3A",
  "user": {
    "id": 164,
    "name": "Nakum Vrajkumar Rajeshbhai",
    "email": "24010101666@darshan.ac.in",
    "role": "admin",
    "clinicId": 337,
    "clinicName": "Nakum Vrajkumar Rajeshbhai Clinic",
    "clinicCode": "CLINIC-337"
  }
}
Response headers
 access-control-allow-credentials: true 
 access-control-allow-origin: https://cmsback.sampaarsh.cloud 
 connection: keep-alive 
 content-length: 392 
 content-type: application/json; charset=utf-8 
 date: Tue,17 Mar 2026 03:30:17 GMT 
 etag: W/"188-vvaolkedna8MLK4IHx1kpnMxL9I" 
 server: nginx/1.24.0 (Ubuntu) 
 vary: Origin 
 x-powered-by: Express 
Responses
Code	Description	Links
200	
Success

Media type

application/json
Controls Accept header.
Example Value
Schema
{
  "token": "string",
  "user": {
    "id": 0,
    "name": "string",
    "email": "string",
    "role": "patient",
    "clinicId": 0,
    "clinicName": "string",
    "clinicCode": "string"
  }
}
No links
400	
Validation error

Media type

application/json
Example Value
Schema
{
  "error": "Error message"
}
No links
401	
Invalid email or password

Media type

application/json
Example Value
Schema
{
  "error": "Error message"
}
No links
Appointments
Patient: book and view appointments



POST
/appointments
Book appointment (Patient)


Creates appointment and queue entry. Status set to queued. Date must be today or future; time slot unique per clinic per date.

Parameters
Try it out
No parameters

Request body

application/json
Example Value
Schema
{
  "appointmentDate": "2025-03-10",
  "timeSlot": "10:00-10:15"
}
Responses
Code	Description	Links
201	
Appointment with queueEntry

Media type

application/json
Controls Accept header.
Example Value
Schema
{
  "id": 0,
  "appointmentDate": "2026-03-17",
  "timeSlot": "string",
  "status": "scheduled",
  "patientId": 0,
  "clinicId": 0,
  "createdAt": "2026-03-17T03:34:57.024Z",
  "queueEntry": {
    "id": 0,
    "tokenNumber": 0,
    "status": "waiting",
    "queueDate": "2026-03-17",
    "appointmentId": 0,
    "appointment": {
      "patient": {
        "name": "string",
        "phone": "string"
      }
    }
  }
}
No links
400	
Validation or time slot already booked

Media type

application/json
Example Value
Schema
{
  "error": "Error message"
}
No links
401	
Token missing or invalid

No links
403	
Forbidden

No links

GET
/appointments/my
My appointments (Patient)


List appointments for logged-in patient. Includes queueEntry (token, status).

Parameters
Try it out
No parameters

Responses
Code	Description	Links
200	
List of appointments

Media type

application/json
Controls Accept header.
Example Value
Schema
[
  {
    "id": 0,
    "appointmentDate": "2026-03-17",
    "timeSlot": "string",
    "status": "scheduled",
    "patientId": 0,
    "clinicId": 0,
    "createdAt": "2026-03-17T03:34:57.025Z",
    "queueEntry": {
      "id": 0,
      "tokenNumber": 0,
      "status": "waiting",
      "queueDate": "2026-03-17",
      "appointmentId": 0,
      "appointment": {
        "patient": {
          "name": "string",
          "phone": "string"
        }
      }
    }
  }
]
No links
401	
Token missing or invalid

No links
403	
Forbidden

No links

GET
/appointments/{id}
Appointment details with prescription and report (Patient)


Single appointment by ID. Includes prescription (medicines array) and report if present.

Parameters
Try it out
Name	Description
id *
integer
(path)
id
Responses
Code	Description	Links
200	
Appointment with prescription and report

No links
400	
Invalid id

No links
401	
Token missing or invalid

No links
403	
Forbidden

No links
404	
Appointment not found

No links
Queue
Receptionist: daily queue and status updates



GET
/queue
Daily queue (Receptionist)


Queue for date. Query: date=YYYY-MM-DD. Sorted by token. Includes appointment.patient (name, phone).

Parameters
Try it out
Name	Description
date *
string($date)
(query)
2025-03-10
Responses
Code	Description	Links
200	
List of queue entries

Media type

application/json
Controls Accept header.
Example Value
Schema
[
  {
    "id": 0,
    "tokenNumber": 0,
    "status": "waiting",
    "queueDate": "2026-03-17",
    "appointmentId": 0,
    "appointment": {
      "patient": {
        "name": "string",
        "phone": "string"
      }
    }
  }
]
No links
400	
Valid date required (YYYY-MM-DD)

No links
401	
Token missing or invalid

No links
403	
Forbidden

No links

PATCH
/queue/{id}
Update queue status (Receptionist)


Body: { status: "in-progress" | "done" | "skipped" }. Transitions: waiting->in-progress or skipped; in_progress->done.

Parameters
Try it out
Name	Description
id *
integer
(path)
id
Request body

application/json
Example Value
Schema
{
  "status": "in-progress"
}
Responses
Code	Description	Links
200	
Updated queue entry

No links
400	
Invalid status or transition

No links
401	
Token missing or invalid

No links
403	
Forbidden

No links
404	
Queue entry not found

No links
Doctor
Doctor: today's queue



GET
/doctor/queue
Today's queue (Doctor)


Queue for today. Returns id, tokenNumber, status, patientName, patientId, appointmentId. Use appointmentId for prescription/report.

Parameters
Try it out
No parameters

Responses
Code	Description	Links
200	
List

Media type

application/json
Controls Accept header.
Example Value
Schema
[
  {
    "id": 0,
    "tokenNumber": 0,
    "status": "string",
    "patientName": "string",
    "patientId": 0,
    "appointmentId": 0
  }
]
No links
401	
Token missing or invalid

No links
403	
Forbidden

No links
Prescriptions
Doctor: add; Patient: list own



GET
/prescriptions/my
My prescriptions (Patient)


All prescriptions for patient. Each has medicines (name, dosage, duration), notes, doctor, appointment.

Parameters
Try it out
No parameters

Responses
Code	Description	Links
200	
List of prescriptions

No links
401	
Token missing or invalid

No links
403	
Forbidden

No links

POST
/prescriptions/{appointmentId}
Add prescription (Doctor)


One per appointment. Only for appointment status in_progress or done. appointmentId from GET /doctor/queue.

Parameters
Try it out
Name	Description
appointmentId *
integer
(path)
appointmentId
Request body

application/json
Example Value
Schema
{
  "medicines": [
    {
      "name": "Paracetamol",
      "dosage": "500mg",
      "duration": "5 days"
    }
  ],
  "notes": "After food"
}
Responses
Code	Description	Links
201	
Prescription created

No links
400	
Validation or duplicate or wrong status

No links
401	
Token missing or invalid

No links
403	
Forbidden

No links
404	
Appointment not found

No links
Reports
Doctor: add; Patient: list own



GET
/reports/my
My reports (Patient)


Medical reports for patient. diagnosis, testRecommended, remarks, doctor, appointment.

Parameters
Try it out
No parameters

Responses
Code	Description	Links
200	
List of reports

No links
401	
Token missing or invalid

No links
403	
Forbidden

No links

POST
/reports/{appointmentId}
Add report (Doctor)


appointmentId from GET /doctor/queue.

Parameters
Try it out
Name	Description
appointmentId *
integer
(path)
appointmentId
Request body

application/json
Example Value
Schema
{
  "diagnosis": "Viral Fever",
  "testRecommended": "Blood Test",
  "remarks": "Rest for 3 days"
}
Responses
Code	Description	Links
201	
Report created

No links
400	
Validation failed

No links
401	
Token missing or invalid

No links
403	
Forbidden

No links
404	
Appointment not found

No links
Admin
Admin: clinic info and create users



GET
/admin/clinic
Clinic info (Admin)


Clinic name, code, userCount, appointmentCount, queueCount.

Parameters
Try it out
No parameters

Responses
Code	Description	Links
200	
Clinic object

No links
401	
Token missing or invalid

No links
403	
Forbidden

No links
404	
Clinic not found

No links

GET
/admin/users
List users (Admin)


All users in clinic. id, name, email, role, phone, createdAt.

Parameters
Try it out
No parameters

Responses
Code	Description	Links
200	
List of users

No links
401	
Token missing or invalid

No links
403	
Forbidden

No links

POST
/admin/users
Create user (Admin)


Create receptionist, patient, or doctor. They sign in with given email/password.

Parameters
Try it out
No parameters

Request body

application/json
Example Value
Schema
{
  "name": "John Doe",
  "email": "user@example.com",
  "password": "string",
  "role": "receptionist",
  "phone": "string"
}
Responses
Code	Description	Links
201	
User created (no password)

No links
400	
Validation or email in use

No links
401	
Token missing or invalid

No links
403	
Forbidden

No links

Schemas
Error{
error	string
example: Error message
}
LoginRequest{
email*	string($email)
example: doctor1@clinic1.local
password*	string
minLength: 1
example: password123
}
LoginResponse{
token	string
JWT for Authorization header

user	{
id	integer
name	string
email	string
role	string
Enum:
[ patient, doctor, receptionist, admin ]
clinicId	integer
clinicName	string
clinicCode	string
}
}
BookAppointmentRequest{
appointmentDate*	string($date)
example: 2025-03-10
YYYY-MM-DD, today or future

timeSlot*	string
example: 10:00-10:15
Format HH:MM-HH:MM

}
Appointment{
id	integer
appointmentDate	string($date)
timeSlot	string
status	string
Enum:
[ scheduled, queued, in_progress, done, cancelled ]
patientId	integer
clinicId	integer
createdAt	string($date-time)
queueEntry	QueueEntry{
id	integer
tokenNumber	integer
status	string
Enum:
[ waiting, in_progress, done, skipped ]
queueDate	string($date)
appointmentId	integer
appointment	{
patient	{
name	{
}
phone	{
}
}
}
}
}
QueueEntry{
id	integer
tokenNumber	integer
status	string
Enum:
[ waiting, in_progress, done, skipped ]
queueDate	string($date)
appointmentId	integer
appointment	{
patient	{
name	{
}
phone	{
}
}
}
}
QueueUpdateRequest{
status*	string
waiting->in-progress|skipped; in_progress->done

Enum:
[ in-progress, done, skipped ]
}
DoctorQueueItem{
id	integer
tokenNumber	integer
status	string
patientName	string
patientId	integer
appointmentId	integer
}
PrescriptionMedicine{
name*	string
example: Paracetamol
dosage*	string
example: 500mg
duration*	string
example: 5 days
}
AddPrescriptionRequest{
medicines*	[
minItems: 1
PrescriptionMedicine{
name*	string
example: Paracetamol
dosage*	string
example: 500mg
duration*	string
example: 5 days
}]
notes	string
example: After food
}
AddReportRequest{
diagnosis*	string
example: Viral Fever
testRecommended	string
example: Blood Test
remarks	string
example: Rest for 3 days
}
AdminCreateUserRequest{
name*	string
minLength: 3
example: John Doe
email*	string($email)
password*	string
minLength: 6
role*	string
Enum:
[ receptionist, patient, doctor ]
phone	string
}

cover each and everything now start step by step