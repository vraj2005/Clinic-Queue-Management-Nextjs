hii buddy just complete this task and write all code inside it nothing else just do this after i will tell you what to do

# 1️⃣ Final Recommended Project Structure

Inside **app + src folders**

```
app
 ├ login
 │   └ page.tsx
 ├ dashboard
 │   └ page.tsx
 ├ users
 │   └ page.tsx
 ├ users
 │   └ create
 │        └ page.tsx
 ├ layout.tsx
 ├ page.tsx

src
 ├ api
 │   ├ axios.ts
 │   ├ auth.api.ts
 │   ├ admin.api.ts
 │
 ├ components
 │   ├ Navbar.tsx
 │   ├ Loader.tsx
 │
 ├ types
 │   └ user.ts
```

Why this is good:

✔ looks **professional**
✔ clean **separation of concerns**
✔ **not over complicated**

Faculty will like this.

---

# 2️⃣ Axios Setup

📄 `src/api/axios.ts`

```ts
import axios from "axios"

const api = axios.create({
  baseURL: "https://cmsback.sampaarsh.cloud"
})

api.interceptors.request.use((config) => {

  const token = localStorage.getItem("token")

  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }

  return config
})

export default api
```

---

# 3️⃣ Auth API

📄 `src/api/auth.api.ts`

```ts
import api from "./axios"

export const loginUser = async (email:string,password:string) => {

  const res = await api.post("/auth/login",{
    email,
    password
  })

  return res.data
}
```

---

# 4️⃣ Admin API

📄 `src/api/admin.api.ts`

```ts
import api from "./axios"

export const getClinicInfo = async () => {

  const res = await api.get("/admin/clinic")

  return res.data
}

export const getUsers = async () => {

  const res = await api.get("/admin/users")

  return res.data
}

export const createUser = async (data:any) => {

  const res = await api.post("/admin/users",data)

  return res.data
}
```

---

# 5️⃣ Login Page

📄 `app/login/page.tsx`

```tsx
"use client"

import { useState } from "react"
import { useRouter } from "next/navigation"
import { loginUser } from "@/src/api/auth.api"

export default function LoginPage(){

  const router = useRouter()

  const [email,setEmail] = useState("")
  const [password,setPassword] = useState("")

  const handleLogin = async () => {

    try{

      const data = await loginUser(email,password)

      localStorage.setItem("token",data.token)
      localStorage.setItem("user",JSON.stringify(data.user))

      router.push("/dashboard")

    }catch(err){
      alert("Invalid credentials")
    }

  }

  return(

    <div style={{padding:"40px"}}>

      <h2>Clinic CMS Login</h2>

      <div style={{marginTop:"20px"}}>

        <input
          placeholder="Email"
          value={email}
          onChange={(e)=>setEmail(e.target.value)}
        />

      </div>

      <div style={{marginTop:"10px"}}>

        <input
          type="password"
          placeholder="Password"
          value={password}
          onChange={(e)=>setPassword(e.target.value)}
        />

      </div>

      <button
        onClick={handleLogin}
        style={{marginTop:"15px"}}
      >
        Login
      </button>

    </div>

  )
}
```

---

# 6️⃣ Dashboard (Clinic Info)

📄 `app/dashboard/page.tsx`

```tsx
"use client"

import { useEffect,useState } from "react"
import { getClinicInfo } from "@/src/api/admin.api"

export default function Dashboard(){

  const [clinic,setClinic] = useState<any>(null)

  const loadClinic = async () => {

    const data = await getClinicInfo()

    setClinic(data)
  }

  useEffect(()=>{
    loadClinic()
  },[])

  if(!clinic) return <p>Loading...</p>

  return(

    <div style={{padding:"30px"}}>

      <h2>Clinic Dashboard</h2>

      <p><b>Name:</b> {clinic.name}</p>
      <p><b>Code:</b> {clinic.code}</p>
      <p><b>Total Users:</b> {clinic.userCount}</p>
      <p><b>Total Appointments:</b> {clinic.appointmentCount}</p>
      <p><b>Total Queue:</b> {clinic.queueCount}</p>

    </div>

  )
}
```

---

# 7️⃣ Users List Page

📄 `app/users/page.tsx`

```tsx
"use client"

import { useEffect,useState } from "react"
import { getUsers } from "@/src/api/admin.api"

export default function UsersPage(){

  const [users,setUsers] = useState<any[]>([])

  const loadUsers = async () => {

    const data = await getUsers()

    setUsers(data)
  }

  useEffect(()=>{
    loadUsers()
  },[])

  return(

    <div style={{padding:"30px"}}>

      <h2>Clinic Users</h2>

      <table border={1} cellPadding={10}>

        <thead>
          <tr>
            <th>Name</th>
            <th>Email</th>
            <th>Role</th>
            <th>Phone</th>
          </tr>
        </thead>

        <tbody>

          {users.map((u)=>(
            <tr key={u.id}>
              <td>{u.name}</td>
              <td>{u.email}</td>
              <td>{u.role}</td>
              <td>{u.phone}</td>
            </tr>
          ))}

        </tbody>

      </table>

    </div>

  )
}
```

---

# 8️⃣ Create User Page

📄 `app/users/create/page.tsx`

```tsx
"use client"

import { useState } from "react"
import { createUser } from "@/src/api/admin.api"

export default function CreateUser(){

  const [form,setForm] = useState({
    name:"",
    email:"",
    password:"",
    role:"doctor",
    phone:""
  })

  const handleChange = (e:any) => {

    setForm({
      ...form,
      [e.target.name]:e.target.value
    })
  }

  const submit = async () => {

    await createUser(form)

    alert("User created")
  }

  return(

    <div style={{padding:"30px"}}>

      <h2>Create User</h2>

      <input name="name" placeholder="Name" onChange={handleChange}/>
      <br/><br/>

      <input name="email" placeholder="Email" onChange={handleChange}/>
      <br/><br/>

      <input name="password" placeholder="Password" onChange={handleChange}/>
      <br/><br/>

      <input name="phone" placeholder="Phone" onChange={handleChange}/>
      <br/><br/>

      <select name="role" onChange={handleChange}>
        <option value="doctor">Doctor</option>
        <option value="receptionist">Receptionist</option>
        <option value="patient">Patient</option>
      </select>

      <br/><br/>

      <button onClick={submit}>
        Create
      </button>

    </div>

  )
}
```

---

# 9️⃣ What We Have Covered

✔ Login
✔ Token handling
✔ Admin clinic info
✔ List users
✔ Create user

All **Admin endpoints** done.

---

# 🔜 Next APIs We Will Implement

Next step we will add **remaining system modules**:

### Patient

```
Book appointment
My appointments
Appointment details
My prescriptions
My reports
```

### Receptionist

```
Daily queue
Update queue status
```

### Doctor

```
Doctor queue
Add prescription
Add report
```

---

💡 **Important tip for your exam:**

Always show **navigation bar like this**:

```
Dashboard
Users
Create User
Logout
```

Faculty immediately sees **full system working**.

---

